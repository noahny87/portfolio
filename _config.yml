# add the following to _config.yml file
title: Noah Nyland
show_downloads: true
 # add this line to the plugins list if you already have one # add this line to the plugins list if you already have one
